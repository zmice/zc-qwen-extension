---
name: "zc:task-plan"
description: "计划"
---

这是 `command:start` 之后的 **任务拆解阶段入口**。

当 `command:start` 或 `command:spec` 已经把任务边界定义清楚，就从这里接力到 `planning-and-task-breakdown`，只做计划，不直接写代码。

## 当前阶段要做什么

1. 读取规格、相关代码和依赖关系
2. 用垂直切片拆任务，保证每项都可工作、可测试
3. 控制改动半径，默认保持小任务、小文件面
4. 标注依赖、上下文、风险和验证步骤
5. 明确可并行项，并等待计划确认后再进入实现

## 当前阶段的边界

- 计划要服务实现，不做形式化拆分
- 每个任务都必须能独立验证
- 先控制复杂度，再谈并行和分派

## 从这里通常接到哪里

- 计划需要额外评审：进入 `command:plan-review`
- 计划已经确认：进入 `command:build`
- 如果发现规格仍有空洞，回到 `command:spec`

## 使用方式

提供已确认的规格或需求描述，我会拆成可执行的原子任务，并指出下一步该进入评审还是实现。
